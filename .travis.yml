language: python
python:
  - "3.7"
before_install:
  - echo -e "hello" > /tmp/adbkey
  - echo -e "hello" > /tmp/adbkey.pub
  - mv travis_secrets.yaml secrets.yaml
  - mkdir www/deepstack_person_images
install:
  - pip3 install homeassistant
script:
  - hass -c . --script check_config
  
